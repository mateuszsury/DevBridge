# DevBridge Configuration
# Author: Mateusz Sury
# Python 3.13+
# Copy this file to .env and update with your settings

# ============================================
# Database Configuration
# ============================================
DB_PATH=data/devbridge.db

# ============================================
# Bootstrap Admin Account
# ============================================
# These credentials are used ONLY on first run when no users exist
# CHANGE THESE IMMEDIATELY IN PRODUCTION!
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=admin

# ============================================
# Security
# ============================================
# Secret key for session signing (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Session cookie name
SESSION_COOKIE=devbridge_session

# ============================================
# Server Configuration
# ============================================
# Host to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
HOST=127.0.0.1

# Port to run on
PORT=8000

# ============================================
# Application Settings (can also be changed in UI)
# ============================================
# These are defaults, can be overridden in the web UI

# Require authentication for all access
# AUTH_REQUIRED=false

# Allow anonymous users to create terminals (when AUTH_REQUIRED=false)
# ALLOW_ANONYMOUS_TERMINAL=true

# Maximum number of concurrent terminal sessions
# MAX_SESSIONS=50

# Idle session timeout in seconds (0 = disabled)
# IDLE_TTL_SECONDS=0

# Scrollback buffer size in characters
# SCROLLBACK_LIMIT_CHARS=200000

# Default shell for Unix/Linux systems
# DEFAULT_UNIX_SHELL=/bin/bash

# Default shell for Windows systems
# DEFAULT_WINDOWS_SHELL=powershell.exe
